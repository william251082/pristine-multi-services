import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// App component will request on server which not ready yet at this moment
it('renders without crashing', () => {});
